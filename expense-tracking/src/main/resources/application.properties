spring.application.name=expense-tracking

# Database — all values injected via environment variables
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/productivity_db}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:postgres}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# JWT Configuration — injected via environment variable
blaze.app.jwtSecret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
blaze.app.jwtExpirationMs=${JWT_EXPIRATION_MS:86400000}
